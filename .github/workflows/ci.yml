name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check & Test (SQLite-only, musl static)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-musl

      - name: Install musl-tools
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-musl-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --release

      - name: Run tests
        run: cargo test

      - name: Clippy
        run: cargo clippy -- -D warnings

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Upload binary artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: actions/upload-artifact@v4
        with:
          name: otel_dumper-x86_64-musl
          path: target/x86_64-unknown-linux-musl/release/otel_dumper

  check-duckdb:
    name: Check & Test (DuckDB, musl static)
    runs-on: ubuntu-latest
    container:
      image: rust:1-alpine3.21
    steps:
      - name: Install build dependencies
        run: apk add --no-cache musl-dev g++ make git

      - uses: actions/checkout@v4

      - name: Install clippy
        run: rustup component add clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            /usr/local/cargo/registry
            /usr/local/cargo/git
            target
          key: alpine-cargo-duckdb-${{ hashFiles('**/Cargo.lock') }}

      - name: Build
        run: cargo build --release --features duckdb

      - name: Run tests
        run: cargo test --features duckdb

      - name: Clippy
        run: cargo clippy --features duckdb -- -D warnings

      - name: Verify static binary
        run: |
          file target/x86_64-unknown-linux-musl/release/otel_dumper
          # Must be statically linked
          file target/x86_64-unknown-linux-musl/release/otel_dumper | grep -q "statically linked"

      - name: Upload binary artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: actions/upload-artifact@v4
        with:
          name: otel_dumper-x86_64-musl-duckdb
          path: target/x86_64-unknown-linux-musl/release/otel_dumper

  release:
    name: Update Latest Release
    needs: [check, check-duckdb]
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download musl binary
        uses: actions/download-artifact@v4
        with:
          name: otel_dumper-x86_64-musl
          path: ./artifact-musl

      - name: Download musl+duckdb binary
        uses: actions/download-artifact@v4
        with:
          name: otel_dumper-x86_64-musl-duckdb
          path: ./artifact-duckdb

      - name: Prepare binaries
        run: |
          chmod +x ./artifact-musl/otel_dumper
          mv ./artifact-musl/otel_dumper ./otel_dumper-x86_64-musl
          chmod +x ./artifact-duckdb/otel_dumper
          mv ./artifact-duckdb/otel_dumper ./otel_dumper-x86_64-musl-duckdb

      - name: Delete old latest release
        run: gh release delete latest --yes --cleanup-tag || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Latest Build
          body: |
            Automatically built from `master` branch (commit ${{ github.sha }}).

            ## Download

            | Binary | Description |
            |--------|-------------|
            | `otel_dumper-x86_64-musl` | Static musl binary (SQLite only, runs anywhere) |
            | `otel_dumper-x86_64-musl-duckdb` | Static musl binary with DuckDB + SQLite support |
          draft: false
          prerelease: false
          make_latest: true
          files: |
            otel_dumper-x86_64-musl
            otel_dumper-x86_64-musl-duckdb
